const clearLocalStorage = () => {
  localStorage.clear();
  console.log("LocalStorage cleared successfully.");
};

// Call the clearLocalStorage function wherever you need to clear localStorage
// For example, you can call it in your component or any event handler
clearLocalStorage();
